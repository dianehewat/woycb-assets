# Make.com Scenario

`woycb-make-blueprint-sanitised.json` is a sanitised export of the working Make.com scenario used during the hackathon.

The submission copy preserves the workflow architecture while removing credentials, participant information and private resource identifiers.

It is supplied as implementation evidence, not as a one-click deployable package. Connections and private resources must be configured separately.
